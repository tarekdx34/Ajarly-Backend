[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -Dspring.profiles.active=railway -jar target/backend-0.0.1-SNAPSHOT.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/health"
timeout = 100
```

### 2. Create `Procfile` in project root:
```
web: java -Dserver.port=$PORT -Dspring.profiles.active=railway -jar target/backend-0.0.1-SNAPSHOT.jar